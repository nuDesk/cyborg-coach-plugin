# 01 — Compatibility pointer

This pointer is retained intentionally so older references to module 01 continue to resolve without restoring the former curriculum.

The active guidance has been consolidated into `${CLAUDE_PLUGIN_ROOT}/references/learning-path.md`. Read "Choosing where to start" there for routing and for shaping the useful result.

The response rule and boundaries remain in `${CLAUDE_PLUGIN_ROOT}/references/operating-rules.md`.
